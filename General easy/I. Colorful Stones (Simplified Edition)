#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
    string s1 ,s2; cin >> s1 >> s2;
    ll cnt = 0;
    ll n = s2.size();
    for (ll i = 0; i < n; i++) {
        if (s2[i] == s1[cnt]) {cnt +=1;}
    }
    cout << cnt+1 ;
    }


int main() {
    George;
    int t = 1 ;
    //cin >> t;

    while (t--)
        solve();

}
