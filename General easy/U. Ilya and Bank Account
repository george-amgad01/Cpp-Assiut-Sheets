#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
    string s; cin >> s;
    ll n = s.length();
    if (s[0]!='-') {cout << s ;}
    else  {
        if (s[n-1] >= s[n-2]) {s.replace(n-1,1,"");}
        else {s.replace(n-2,1,"");}
        ll num = stoi(s);
        cout << num ;
    }
    }

int main() {
    George;
    int t = 1 ;
    //cin >> t;

    while (t--)
        solve();

}
