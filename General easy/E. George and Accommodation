#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
    ll n ; cin >> n;
    ll cnt = 0 ;
    for (int i = 0; i < n; i++) {
        ll p ,q ; cin >> p >> q;
        if (q-p >= 2 ) {cnt +=1 ;}
    }
        cout << cnt ;
}

int main() {
    George;
    int t =1 ;
    //cin >> t;
    while (t--) {
        solve();
    }
}
