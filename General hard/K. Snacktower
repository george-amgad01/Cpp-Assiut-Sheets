#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define dd double
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)


void solve () {
    int n ; cin >> n ;
    int mx = n , cnt = 0  ;
    vector<int> a(n);
    map <int,int> mp;
    for (int i = 0 ; i < n ; ++i) cin >> a[i];
    for (int i = 0 ; i < n ; ++i ) {
        mp[a[i]]++ ;
        if (a[i] == mx ) {
            int x = mx ;
            while (mp[x] > 0 ) {
                cout << x << " " ;
                mx -- ;
                x-- ;
            }
        }
            cout << nl ;
    }



}
// 33 -- 65
int main() {
    George;
    int t =1 ;
    //cin >> t;
    while (t--) {
        solve();
    }
}
