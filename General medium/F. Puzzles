#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std ;
int main () {
    ll n , m ; cin >> n >> m ;
    ll arr [m]  ;
    ll arr2 [(m-n)+1] ;
    for (int i = 0 ; i < m ; ++i ) {
        cin >> arr[i] ;
    }
    sort(arr,arr+m) ;
    for (int i = 0 ; i < m ; ++i) {
        if (i+n-1 >= m ) {break;}
        ll min = arr[(i+n)-1] - arr [i] ;
        arr2[i] = min ;
    }
    cout << *min_element(arr2,arr2 + (m-n)+1 ) ;
}
