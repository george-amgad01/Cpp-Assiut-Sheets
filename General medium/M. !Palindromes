#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
    string s ,s2 ; cin >> s;
    s2 = s ; reverse(s2.begin(),s2.end());
    ll n = s.size();
    set<char> st;
    for (auto e : s ) {
        st.insert(e);
    }
    if (st.size() == 1) {cout<< 0 ;}
    else if (s != s2) {cout << n ;}
    else {
        cout << n -1 ;
    }}

int main() {
    George;
    int t =1 ;
    //cin >> t;
    while (t--) {
        solve();
    }
}
