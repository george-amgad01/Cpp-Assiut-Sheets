#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}
void prime (ll n,bool f) {
    if (n < 2 ) {cout << "NO" << nl ; return;}
    for (ll i = 2 ; i*i <= n ; i++) {
        if (n%i == 0) {
            f = 1 ;
            break ;
        }
    }
    cout << (f == 0 ? "YES" : "NO") << nl ;
}

int main() {
    George;
    int t ;
    cin >> t;
    while (t--) {
        ll n ;
        cin >> n ;
        bool f = 0 ;
        prime(n,f) ;
        solve();

    }
}
