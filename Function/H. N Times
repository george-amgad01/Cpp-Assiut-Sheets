#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)

void n_time (ll n , char c) {

    for (ll i = 0 ; i < n ; i++) {
        if (i == n-1) cout << c;
        else cout << c << " " ;
    }
    cout << nl ;
}

int main() {
    George;
    int t  ;
    cin >> t;
    while (t--) {
        ll n ;
        char c ;
        cin >> n >> c ;
        n_time(n,c) ;

    }
}
