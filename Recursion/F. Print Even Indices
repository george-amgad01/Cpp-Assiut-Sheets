#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}
void even_ind  (ll arr [] ,ll n) {
    if (n==0) {
        return ;
    }
    if ( (n-1)%2 == 0) {           // 7-1 = 6  %2 = 0 ; arr[6] -- 4 arr[4]
        cout << arr[n-1] << " " ;
    }
     return even_ind(arr,n-1);
}


int main() {
    George;
    int t =1  ;
    //cin >> t;

    ll n ; cin >> n ;
    ll arr[n] ;
    for (ll i = 0 ; i < n ; i++) {
        cin >> arr[i] ;
    }
    while (t--) {

        even_ind(arr,n) ;
        solve();
    }
}
