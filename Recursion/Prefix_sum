#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {

}
void prefix_sum (vector<ll>& v , ll n , ll goal) {
    static ll res = 0  ;
    if (n < 0  ) { // 1 2
        return ;
    }

    prefix_sum(v, n-1, goal);
    if (n < goal  ) {
        res += v[n];
    }
    else if (n == goal ) {
        cout << res ;
    }
}



int main() {
    George;
    int t =1 ;
    //cin >> t;
    ll n , goal ; cin >> n >> goal ;
    vector <ll> v(n);
    for (ll i = 0 ; i < n ; i++) {
        cin >> v[i];
    }
    while (t--) {
        cout<< nl ;
        prefix_sum (v , n, goal);
        solve();
    }
}
