#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
}

ll factorial ( ll n ) {
    if (n == 1 ) {
        return 1 ;
    }
    return n * factorial ( n - 1 ) ;
}


int main() {
    George;
    int t = 1;
    //cin >> t;
    while (t--) {
        ll n ; cin >> n;
        cout << factorial(n) ;
        solve();
    }
}
