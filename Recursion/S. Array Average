#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {

}
double average_arr(vector<double>& v,ll n) {

    if (n == 1) {
        return v[0] ;
    }
    return v[n-1] + average_arr(v,n-1) ;
}

int main() {
    George;
    int t =1  ;
    //cin >> t;
    cout << fixed << setprecision(6);
    while (t--) {
        ll n ; cin >> n;
        vector <double> v(n);
        for (ll i = 0 ; i < n ; i++ ) {
            cin >> v[i];
        }
        cout << fixed << average_arr(v,n)/ n ;
        solve();
    }
}
