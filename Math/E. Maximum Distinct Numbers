#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define George ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0)
void solve() {
    ll n ;cin >> n ;
    n*=2 ;
    ll sq = sqrt(n);
    for ( ll i = sq - 2  ; i < sq + 20 ; ++i ) {
        if (i * (i +1) > n) {
            cout << i -1 ;
            break;
        }
    }

}
int main() {
    George;
    int t =1   ;
    //cin >> t;
    while (t--) {

        solve();

    }
}
